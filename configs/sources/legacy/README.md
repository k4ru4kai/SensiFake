# Legacy collector configurations

`openfake-1000.toml` is the pre-pilot 1,000-image target configuration. It is
retained for provenance only. New OpenFake collection uses
`configs/sources/openfake.toml`.
